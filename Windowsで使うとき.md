# Windowsで利用される方へ

このフォルダはMacとWindowsのどちらでも利用できます。Windowsをお使いの場合は、はじめにこのページをご確認ください。

---

## 実行するファイルについて

実行用のファイルはMac用とWindows用で分かれています。

- 名前の末尾に **「.command」と付くファイルはMac用** です。Windowsでは動作しません
- Windowsでは、**同じ名前で「.command」が付かないファイル**(種類に「Windowsバッチファイル」と表示されるもの)をダブルクリックしてください

| 手順書での表記 | Windowsで実行するファイル |
|---|---|
| はじめにこれ.command | はじめにこれ(Windowsバッチファイル) |
| 実行する.command | 実行する(Windowsバッチファイル) |
| ①取得する.command | ①取得する(Windowsバッチファイル) |
| ②集計する.command | ②集計する(Windowsバッチファイル) |

---

## 初回の準備(1回のみ)

### 1. ZIPで受け取った場合は、展開前にブロック解除を行う

ダウンロードしたZIPファイルを **右クリック →「プロパティ」** で開き、下部に **「許可する」というチェック欄** があればチェックを入れて「OK」を押してください。そのうえで **右クリック →「すべて展開」** から展開します。
(この操作により、実行時のセキュリティ警告を減らせます。チェック欄が表示されない場合は、そのまま展開して問題ありません)

### 2. Python(実行に必要なソフト)について

Pythonは **「はじめにこれ」が自動でインストールする** ため、通常は手動での作業は不要です。

「はじめにこれ」の実行時に「Pythonの自動インストールができませんでした」と表示された場合のみ、次の手順でインストールしてください。

1. ブラウザで https://www.python.org/downloads/ を開く
2. **「Download Python 3.◯◯」ボタン** からインストーラーをダウンロードする
3. ダウンロードしたファイルを実行する
4. 最初の画面で、下部の **「Add python.exe to PATH」に必ずチェック** を入れる(チェックを忘れるとプログラムが実行できません)
5. **「Install Now」** を押し、完了後「Close」で閉じる

### 3. Google Chromeの確認(「毎日の取得」を行う場合のみ)

データ取得にChromeを使用します。未インストールの場合は https://www.google.com/chrome/ からインストールしてください。

### 4. 「はじめにこれ」を実行する

作業するフォルダ(「毎日の取得」または「月次集計-WL」)内の **「はじめにこれ」(Windowsバッチファイル)** をダブルクリックします。
画面に「準備完了」と表示されたら準備は完了です。以降は各フォルダの「かんたん手順書.md」に沿って進めてください。

---

## セキュリティの確認画面が出て実行できない場合

社用PCでは、ダブルクリック時にセキュリティの確認画面が表示されることがあります。表示内容ごとの対処は次のとおりです。

### A. 青い画面で「WindowsによってPCが保護されました」と表示される

1. 画面内の **「詳細情報」** をクリックする
2. 右下に表示される **「実行」ボタン** を押す

### B. 「セキュリティの警告」ダイアログが表示される

そのまま **「実行」** を押せば進められます。
毎回表示される場合は、ファイルを **右クリック →「プロパティ」→ 下部の「許可する」にチェック → OK** とすると、以降は表示されなくなります。

### C. ダブルクリックしても反応がない・画面が一瞬で閉じる

ダブルクリックでの実行が制限されている可能性があります。次の手順をお試しください。

1. 実行したいファイルが入っているフォルダを開く
2. フォルダ上部の **アドレスバーをクリック** し、**「cmd」と入力してEnter** を押す
3. コマンドプロンプト(黒い画面)が開く
4. 実行したいファイルを **コマンドプロンプトへドラッグ&ドロップ** する
5. ファイルのパスが表示されるので、**Enter** を押すと実行されます

### D. バッチファイル自体の実行が制限されている場合(最終手段)

コマンドを直接貼り付けて実行する方法があります。後述の **「コマンドで直接実行する方法」** をご覧ください。内山と連絡を取りながら進めていただくことをおすすめします。

### E. 上記のいずれでも実行できない場合

セキュリティソフトによってブロックされている可能性があります。
**表示されている画面のスクリーンショットを添えて、内山までご連絡ください。**(社内のIT担当への解除依頼が必要になる場合があります)

---

## コマンドで直接実行する方法(最終手段)

バッチファイルの実行が制限されている環境でも、コマンドを直接貼り付ければ各作業を実行できます。

- **コマンドプロンプトの開き方**: **Windowsキー + R** を押す → 「cmd」と入力 → Enter
- **貼り付け方**: 以下の枠内のコマンドを1行ずつコピーし、コマンドプロンプト上で **右クリック**(貼り付けされます)→ **Enter**。上から順に実行します

**どの作業でも、最初に次の3行を実行します:**

```
chcp 65001
set PYTHONUTF8=1
cd /d "フォルダの場所"
```

3行目の「フォルダの場所」は、エクスプローラーで作業フォルダ(「毎日の取得」または「月次集計-WL」)を開き、アドレスバーをクリックすると表示されるパスに置き換えてください。
(例: `cd /d "C:\Users\taro\Desktop\ats-processors\毎日の取得"`)

### 初回の準備(「はじめにこれ」の代替・1回のみ)

(Pythonが未インストールの場合は、先に「初回の準備」の2に沿って手動でインストールしてください)

「毎日の取得」の場合:

```
py -3 -m venv --clear プログラム本体\venv
プログラム本体\venv\Scripts\python.exe -m pip install pandas pyyaml selenium webdriver-manager playwright
プログラム本体\venv\Scripts\python.exe -m playwright install chromium
mklink /J "プログラム本体\output" output
mkdir プログラム本体\herp\output_herp_jobs
mklink /J "output\herp" プログラム本体\herp\output_herp_jobs
```

(後半3行は「すでに存在します」等のメッセージが表示されても問題ありません。そのまま次へ進んでください)

「月次集計-WL」の場合:

```
py -3 -m venv --clear プログラム本体\venv
プログラム本体\venv\Scripts\python.exe -m pip install playwright
プログラム本体\venv\Scripts\python.exe -m playwright install chromium
```

### 毎日のデータ取得(「実行する」の代替)

「毎日の取得」フォルダで、まず:

```
cd プログラム本体
```

続けて、**当日の曜日のコマンド** を上から順に実行します。

**月曜**:
```
venv\Scripts\python.exe ats.py --media hrmos
venv\Scripts\python.exe rikunabi.py
cd herp
..\venv\Scripts\python.exe herp.py
cd ..
```

**火曜・木曜**:
```
venv\Scripts\python.exe ats.py --media hrmos
venv\Scripts\python.exe ats.py --media talentio
```

**水曜・金曜**:
```
venv\Scripts\python.exe ats.py --media hrmos
venv\Scripts\python.exe ats.py --media jobcan
```

出力ファイルは「毎日の取得」内の **「output」** フォルダ(見当たらない場合は「プログラム本体」内の「output」)に媒体別に保存されます。当日日付のファイルをDriveへアップロードしてください。

### 月次① データ取得(「①取得する」の代替)

「月次集計-WL」フォルダで:

```
cd プログラム本体\atGP
..\venv\Scripts\python.exe scrape_atgp.py
cd ..\LITALICO
..\venv\Scripts\python.exe scrape_snabi_jobs.py
cd ..\BABNAVI
..\venv\Scripts\python.exe scraper.py
cd ..\マイナビパートナーズ
..\venv\Scripts\python.exe scraper.py
cd ..\..
```

### 月次② 集計(「②集計する」の代替)

dodaチャレンジの貼り付け(かんたん手順書の「その②」)を済ませてから、「月次集計-WL」フォルダで:

```
copy /y dodaここに貼る.txt プログラム本体\doda-challenge\doda-challenge.txt
cd プログラム本体\doda-challenge
..\venv\Scripts\python.exe parse_jobs.py
cd ..
venv\Scripts\python.exe monthly_dedup.py
cd ..
```

完了後、「プログラム本体」内に作成された **「日付_集計」フォルダ**(例: 20260630_集計)をエクスプローラーで「月次集計-WL」直下へ移動し、中に **ファイルが7件** あることを確認のうえ、フォルダごとDriveへアップロードしてください。

---

## 補足

- 実行中は、コマンドプロンプトの画面や自動で開いたブラウザを閉じずにお待ちください
- 処理の最後に **「続行するには何かキーを押してください . . .」** と表示されたら完了の合図です。任意のキーを押すと画面が閉じます

---

## 連絡先

- 担当: 内山

(作成: 2026-06-05)
